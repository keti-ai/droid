# /app/droid/fairo/polymetis/arm_polymetis_env.yml
name: polymetis-local
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8.*       # ★ 반드시 3.8 고정 (cp38)
  - pip
  - cmake
  - eigen=3.3
  - ninja
  - pkg-config
  - yaml-cpp
  - urdfdom
  - urdfdom_headers
  - poco
  - assimp
  - openmpi
  - doxygen
  - graphviz
  - zstd
  - libprotobuf
  - protobuf
  - pinocchio
  # pip 패키지는 비워두거나 최소화 (아래 RUN pip ...에서 설치)
